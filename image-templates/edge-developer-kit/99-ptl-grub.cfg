# PTL (Panther Lake) Platform GRUB Configuration
# Required for Intel Core Ultra 3xx series (PTL) platforms
# From: apply_xe_ptl_fix() in gpu_installer.sh

# Enable xe driver probe for PTL integrated GPU
GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} xe.force_probe=b08f"
